# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database (MariaDB / SQLAlchemy / Alembic)
SQLAlchemy==2.0.36
PyMySQL==1.1.1
alembic==1.14.0

# MongoDB
pymongo==4.10.1

# Cache / queue
redis==5.2.1
celery==5.4.0

# Auth & security
# bcrypt pinned to 4.0.1: passlib 1.7.4 reads bcrypt.__about__.__version__,
# which was removed in bcrypt 4.1+ (harmless warning otherwise).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1

# PDF (used from Phase 1 submittal generation onward)
pypdf==5.1.0
reportlab==4.2.5

# Utilities
python-dotenv==1.0.1
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
